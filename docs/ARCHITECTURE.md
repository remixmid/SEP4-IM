# Initial Architecture

```text
React and Vite frontend
        |
        +-- Mock IoT service (planned)
        +-- Rule-based mock ML service (planned)
```

The user interface will depend on stable service contracts rather than importing mock data directly into pages. This allows real APIs to replace the mock implementations later.
